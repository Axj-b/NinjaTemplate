#include <stdio.h>
#include "AddOnNode/AddOnNode.h"

int main(int argv[], char *args[])
{
    int i, j, n;
    printf("Enter the value of n: ");
    scanf("%d", &n);
    printf("you entry: %d\n", n);
    return 0;
}